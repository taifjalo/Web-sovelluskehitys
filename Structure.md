student-restaurants/
│
├── index.html # الصفحة الرئيسية (قائمة المطاعم)
├── restaurant.html # تفاصيل مطعم واحد (اختياري)
│
├── css/
│ └── style.css # التنسيق العام
│
├── js/
│ ├── app.js # main logic (fetch + render UI)
│ ├── api.js # دوال للتعامل مع الـ REST API
│ ├── auth.js # تسجيل الدخول / تسجيل حساب
│ ├── favorites.js # إدارة المفضلة
│ └── map.js # خريطة (OpenStreetMap مثلاً)
│
├── images/
│ └── logo.png
│
└── data/
└── mock.json # بيانات وهمية للاختبار (اختياري)
